---
import Container from '@/components/ui/Container.astro';
import OrdersChart from '@/components/ui/OrdersChart.astro';
import ProductSalesChart from '@/components/ui/ProductSalesChart.astro';
import Layout from '@/layouts/Layout.astro';
import ProfileLayout from '@/layouts/ProfileLayout.astro';
import AdminBreadcrumbs from '@/components/Admin/AdminBreadcrumbs.astro';
import { requireAdminAuth } from '@/lib/auth-utils';

const authResult = await requireAdminAuth(Astro);
if (authResult instanceof Response) {
  return authResult;
}
---

<Layout title="Manobal | Analytics Dashboard">
  <ProfileLayout title="Analytics Dashboard">
    <Container width="marginy">
      <AdminBreadcrumbs
        items={[{ label: 'Analytics' }]}
        pageTitle="Order Analytics"
        pageDescription="Track your store's performance and sales metrics"
      />
      <div class="space-y-4">
        <OrdersChart />
        <ProductSalesChart />
      </div>
    </Container>
  </ProfileLayout>
</Layout>
